import { MesAnoExtensoPipe } from './mes-ano-extenso.pipe';

describe('MesAnoExtensoPipe', () => {
  it('create an instance', () => {
    const pipe = new MesAnoExtensoPipe();
    expect(pipe).toBeTruthy();
  });
});
